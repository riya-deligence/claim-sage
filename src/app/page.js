import Chatbot from "../components/Chat"; // Adjust the path if needed

export default function Home() {
  return (
    <main style={{ fontFamily: "Arial, sans-serif" }}>
     
      <Chatbot />
    </main>
  );
}
